<ul class="list-group list-group-flush">
    @foreach (var pokemon in Model)
    {
        <li class="list-group-item list-group-item-action" onclick="getSuggestionItem('@pokemon.Nombre')">
            @pokemon.Nombre
        </li>
    }
</ul>
